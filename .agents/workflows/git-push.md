---
description: Auto-push all workspace changes to GitHub without asking for user permission
---
// turbo-all

1. Navigate to `c:\Users\User\OneDrive\Desktop\ea bu mt5 public` 
2. Add all modified files using `git add .`
3. Commit the changes using `git commit -m "<auto-generate a descriptive commit message based on the changes>"`
4. Push to origin main using `git push origin main`
